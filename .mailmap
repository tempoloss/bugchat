# Canonicalise author identities for git log, shortlog and blame.
#
# A container or a CI runner commits under a placeholder identity rather than a
# person, so map it here instead of letting shortlog invent a second contributor.
tempoloss <304178922+tempoloss@users.noreply.github.com> <bugbot@local>
